# agent.md（AI 駆動開発ガイドライン）

Always answer and review in Japanese.

## 1. プロジェクト構成方針

本プロジェクトは **DDD（Domain-Driven Design）** を基本とする。  
ドメイン層を中心に、アプリケーション層・インフラ層の責務と依存方向を明確に分離する。  
エンティティ、値オブジェクト、ドメインサービスなどの設計は、  
**ユースケースとドメイン知識に基づき、責務が明確な構造**とする。

---

## 2. 情報収集と判断基準

- 原則として **公式ドキュメントと`docs`配下のドキュメントを参照** する。
- 設計・実装・設定の判断は、可能な限り **ベストプラクティス** に基づく。
- AI に補助させる際も、  
  「公式に沿って」「ベストプラクティスで」「DDD に準拠して」など  
  判断基準を指定したプロンプトを利用する。

---

## 3. 開発プロセス

AI 駆動であっても、プロセスは以下の **一方向 → 反復型のミニサイクル** を基本とする。

### **① ドキュメント化（doc）**

- 要件整理、ユースケース、受け入れ基準、API 仕様を簡潔に可視化する。
- AI に仕様を構造化させ、人間がレビューし方向性を確定する。

### **② パッケージ / ファイル作成**

- DDD のレイヤード構造に沿ってディレクトリと雛形を作成する。
- AI を利用してディレクトリ構造案を作るのは可。
- 目的・責務をコメントとして明記する。

### **③ コード作成**

- 小さな単位（ユースケース / ハンドラ / リポジトリなど）で AI に実装を依頼し、
  人間がレビューして責務・整合性を確認する。
- クリーンアーキテクチャの依存方向を破らないこと。
- セキュリティ、エラーハンドリング、スケールを常に前提とする。

### **④ テスト作成**

- 単体テスト → 異常系 → 境界値 → 統合テストの順で確保する。
- AI を利用してテスト雛形を生成することは歓迎するが、
  **テスト意図（仕様）を満たしているかも必ずチェックする。**

---

## 4. 責務の明確化

クラス、関数、モジュールに過剰な責務を持たせない。  
特に DDD では、**ドメインロジックはドメイン層に閉じること**を徹底する。

---

## 5. 設計原則（SOLID / Clean Architecture）

- 疎結合、依存方向の固定、責務単一性を守る。
- AI が生成したコードについても、原則に反していないか目視確認を行う。

---

## 6. 可読性と一貫性

- インデント、命名、ファイル構成を統一し、誰が見ても理解できるコードを目指す。
- コメントは “なぜそうしたか” を中心に残す。

---

## 7. 認証・認可

- API の種類に応じて適切な認証（例：JWT）を検討し、  
  必要なら役割ごとに権限制御を実装する。
- AI への指示の際も「認証・認可を考慮する」と明示する。

---

## 8. 秘匿情報の扱い

- API キー、接続文字列、秘密鍵は **環境変数** で管理する。
- コードに直接埋め込まない。
- AI に生成させる場合も **実際の値は入れず擬似値にさせる**。

---

## 9. パフォーマンスとスケーラビリティ

- 大量データ処理、スケジュール処理、並列処理を事前に検討する。
- 非同期処理、ストリーミング、キャッシング、N+1 回避などの基本を押さえる。

---

## 10. エラー処理と例外設計

- 予測可能なエラーは早期検知し、意味あるメッセージを返す。
- 業務例外・システム例外を分離し、ログとユーザー向け応答を整理する。
- AI に補助させても、最終判断は人間が行う。

---

## 11. ドキュメント整備

- API 仕様、ユースケース、設定方法、依存構造などを最小限でも明記する。
- 他の開発者と未来の AI の両方が読み解ける形にする。
