# Validation Error Messages (Default - English)
validation.required=Required field is missing

# PaymentAmount
validation.paymentAmount.min=Payment amount must be greater than 0
validation.paymentAmount.scale=Payment amount precision is invalid

# IssueDate
validation.issueDate.future=Issue date must be in the past or today

# PaymentDueDate
validation.paymentDueDate.notFuture=Payment due date must be in the future

# InvoiceId
validation.invoiceId.zeroOrNegative=Invoice ID must be greater than 0

# TotalAmount
validation.totalAmount.negative=Total amount must not be negative
validation.totalAmount.scale=Total amount precision is invalid

# Fee
validation.fee.negative=Fee must not be negative
validation.fee.scale=Fee precision is invalid

# TaxAmount
validation.taxAmount.negative=Tax amount must not be negative
validation.taxAmount.scale=Tax amount precision is invalid

# FeeRate
validation.feeRate.scale=Fee rate precision is invalid

# TaxRate
validation.taxRate.scale=Tax rate precision is invalid

# UserId
validation.userId.zeroOrNegative=User ID must be greater than 0

# Email
validation.email.format=Email format is invalid

# Generic messages
validation.maxLength={0} characters or less

# Password
validation.password.length=Password must be at least 8 characters
validation.password.characterTypes=Password must contain at least 3 of the following: uppercase letters, lowercase letters, numbers, special characters

# HashedPassword
validation.hashedPassword.empty=Hashed password must not be empty

# AccessToken
validation.accessToken.format=Access token format is invalid

# PageNumber
validation.pageNumber.min=Page number must be 1 or greater

# PageSize
validation.pageSize.min=Page size must be 1 or greater
validation.pageSize.max=Page size must be 100 or less

# PaymentDueDate (for list)
validation.paymentDueTo.range=Payment due date end must be after the start date

# Date format
validation.date.format=Date format is invalid

